<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Material 3, no action bar (Compose draws everything). System-bar styling is not set here:
         both activities call enableEdgeToEdge() before the first frame, which overrides any XML
         statusBarColor/navigationBarColor on every API level — and under the edge-to-edge
         enforcement that comes with targeting 35+ those window attributes are deprecated and
         generally have no effect (Android 16 removes the opt-out entirely). -->
    <style name="Theme.Pulsar" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Cold-start splash (core-splashscreen). Always the dark cosmic brand surface with the launcher
         mark, regardless of light/dark, then hands off to the app theme. The richer wordmark +
         tagline moment is the in-app Compose SplashScreen. -->
    <style name="Theme.Pulsar.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Pulsar</item>
    </style>
</resources>
